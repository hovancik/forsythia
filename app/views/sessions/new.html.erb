<% provide(:title, 'Login') %>
<div class="form">
  <%= form_for(:session) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Login", class: "button" %>
  <% end %>
</div>
